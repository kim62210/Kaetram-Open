services:
  kaetram:
    build:
      context: .
      dockerfile: Dockerfile
    image: kaetram:latest
    container_name: kaetram
    restart: unless-stopped
    # config.ts:95가 dotenv.load({path: '../../.env', defaults: '../../.env.defaults'})
    # 로 .env 파일을 직접 읽으므로 서버 설정은 호스트 .env를 마운트한다.
    # sirv(클라이언트)는 process.env.HOST를 직접 읽으므로 환경변수로 0.0.0.0 강제.
    environment:
      HOST: '0.0.0.0'
    volumes:
      - ./.env:/app/.env:ro
    networks:
      - infra_default
    depends_on:
      kaetram-mongo:
        condition: service_healthy
    labels:
      - "com.brian-dev.service=kaetram"
      - "com.brian-dev.domain=kaetram.brian-dev.cloud"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:9000/"]
      interval: 30s
      timeout: 5s
      retries: 3

  kaetram-mongo:
    image: mongo:7
    container_name: kaetram-mongo
    restart: unless-stopped
    volumes:
      - kaetram_mongo_data:/data/db
    networks:
      - infra_default
    labels:
      - "com.brian-dev.service=kaetram-mongo"
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

volumes:
  kaetram_mongo_data:

networks:
  infra_default:
    external: true
